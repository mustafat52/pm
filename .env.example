# Copy this file to .env.local for local testing with `vercel dev`.
# In production, set these in Vercel: Project Settings → Environment Variables.
# Never commit real values — this file only shows the shape.

# The password your employee(s) use to sign in to /project-manager
ADMIN_PASSWORD=alefiyah1012

# A long random string used to sign session cookies. Generate one with:
#   openssl rand -hex 32
SESSION_SECRET=changethistoalongrandomstring
